# MongoDB
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/wedding

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# JWT
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d

# Whitelist — comma-separated emails allowed to access the dashboard
AUTH_WHITELIST=admin@example.com,hr@example.com

# CORS — comma-separated allowed origins
CORS_ORIGIN=http://localhost:3000,https://your-dashboard.vercel.app

# Reunion 2026 admin PIN (never expose this in frontend variables)
ADMIN_PIN=replace-with-a-strong-pin

# Identifies this application to the OpenStreetMap Nominatim geocoding API.
GEOCODING_USER_AGENT=ABC1-Reunion/1.0 (contact@example.com)

# Cloudinary (existing)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

#VAPID keys for web push notifications
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_SUBJECT=mailto:admin@yoursite.com
